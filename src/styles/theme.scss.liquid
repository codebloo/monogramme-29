/**
 * Insert your styles with Liquid below.
 *
 * Import additional stylesheets into this sheet using CSS imports:
 * @import url('./global/form.scss');
 */
 
 /* Verona New CSS  */

.breadcrumb{
    border-bottom: none;
	font-family: 'Work Sans', sans-serif;
	font-size:10px;
    font-weight: 600;
	letter-spacing: 0.5px;
    margin: 0 auto;
	max-width: 1100px;
	padding: 30px 0 0;
	text-transform:uppercase;
	
	a{
		color:#5f5f5f;
		padding: 0 5px;
		text-decoration:none;
	}
		
		.current-crumb{
			color:#42843e;
			padding-left:5px;
		}
}

   .main-nav:after{
	   background: linear-gradient(#887039 0%, #e6be76 50%, #887039 100%);
   		bottom: 0;
		content:'';
		display:block;
		height:1px;
	    left: 0; 
	    position: absolute;
		width:100%;
   }
   
	   
   .collection-sorting{
	   color:#6b6b6b;
	font-family: 'Work Sans', sans-serif;
	font-size:10px;
	font-weight:600;
	letter-spacing:0.5px;
   	position:absolute;
	right:15px;
	text-transform:uppercase;
	top:0;
		select{
			background-color:#fff;
			border:1px solid #eee;
			border-radius:0;
			font-family: 'Work Sans', sans-serif;
			font-size:11px;
			text-transform:uppercase;}
		}
		
	.monogram-stamp{
		left:0;
		position:absolute;
		top:0;}	
		
		
